apiVersion: v1
kind: Service
metadata:
  name: kitchen-scheduler-worker
  labels:
    app: kitchen-scheduler-worker
spec:
  selector:
    app: kitchen-scheduler-worker
  ports:
    - name: http-metrics
      port: 9090
      targetPort: 9090
