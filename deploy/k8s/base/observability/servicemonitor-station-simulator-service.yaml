apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: station-simulator-service
  labels:
    release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - dark-kitchen
  selector:
    matchLabels:
      app: station-simulator-service
  endpoints:
    - port: http
      path: /metrics
      interval: 5s
