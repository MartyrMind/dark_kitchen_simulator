apiVersion: v1
kind: Service
metadata:
  name: station-simulator-service
  labels:
    app: station-simulator-service
spec:
  selector:
    app: station-simulator-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
