apiVersion: v1
kind: Service
metadata:
  name: kitchen-service
  labels:
    app: kitchen-service
spec:
  selector:
    app: kitchen-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
